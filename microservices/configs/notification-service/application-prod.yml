# notification-service prod overrides. Credentials injected via Kubernetes Secrets / Vault.
spring:
  data:
    mongodb:
      uri: ${NOTIFICATION_MONGO_URI}
  datasource:
    url: ${NOTIFICATION_DB_URL}
    username: ${NOTIFICATION_DB_USER}
    password: ${NOTIFICATION_DB_PASSWORD}
