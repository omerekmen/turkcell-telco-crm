# ticket-service prod overrides. Credentials injected via Kubernetes Secrets / Vault.
spring:
  datasource:
    url: ${TICKET_DB_URL}
    username: ${TICKET_DB_USER}
    password: ${TICKET_DB_PASSWORD}
