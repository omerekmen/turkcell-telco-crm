# payment-service dev overrides.
# Lettuce macOS DNS issue: use 127.0.0.1 instead of localhost (see lessons.md 2026-06-24).
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_db
    username: payment
    password: payment
  kafka:
    bootstrap-servers: localhost:29092
