# identity-service prod overrides. Credentials injected via Kubernetes Secrets / Vault.
spring:
  datasource:
    url: ${IDENTITY_DB_URL}
    username: ${IDENTITY_DB_USER}
    password: ${IDENTITY_DB_PASSWORD}
