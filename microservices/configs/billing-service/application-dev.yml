# billing-service dev overrides.
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/billing_db
    username: billing
    password: billing
minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
