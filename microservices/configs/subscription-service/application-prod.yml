# subscription-service prod overrides. Credentials injected via Kubernetes Secrets / Vault.
spring:
  datasource:
    url: ${SUBSCRIPTION_DB_URL}
    username: ${SUBSCRIPTION_DB_USER}
    password: ${SUBSCRIPTION_DB_PASSWORD}
