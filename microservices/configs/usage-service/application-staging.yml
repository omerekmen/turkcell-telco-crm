# usage-service staging overrides. Credentials injected via Kubernetes Secrets.
spring:
  datasource:
    url: ${USAGE_DB_URL}
    username: ${USAGE_DB_USER}
    password: ${USAGE_DB_PASSWORD}
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
