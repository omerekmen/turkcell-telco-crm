# Build context: repo root (telco-crm/).
# docker build -f microservices/payment-service/Dockerfile -t payment-service .
FROM eclipse-temurin:21-jdk-alpine AS builder
WORKDIR /workspace

COPY platform/ platform/
COPY microservices/pom.xml                microservices/pom.xml
COPY microservices/payment-service/pom.xml          microservices/payment-service/pom.xml
COPY microservices/payment-service/src              microservices/payment-service/src

# BuildKit cache mount keeps .m2 across rebuilds without baking deps into layers.
# Cache is busted only when platform source or service pom changes.
RUN --mount=type=cache,target=/root/.m2 \
    mvn -f platform/pom.xml install -DskipTests --no-transfer-progress -q && \
    mvn -f microservices/pom.xml -pl payment-service package -DskipTests --no-transfer-progress -q

FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
COPY --from=builder /workspace/microservices/payment-service/target/*.jar app.jar
EXPOSE 9008
ENTRYPOINT ["java", "-jar", "app.jar"]
