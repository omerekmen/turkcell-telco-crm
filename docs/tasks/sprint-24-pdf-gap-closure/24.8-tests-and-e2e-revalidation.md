# 24.8 - Tests and Full E2E Re-Validation

Status: IN PROGRESS (24.8a/b done; blocked on a pre-existing retry defect - see 24.8-run-report.md). Owner: qa. Closes the sprint per the Sprint 14.6 verification pattern.

## Tasks

1. New acceptance ITs (reuse OnboardingSteps/GatewayApi/TokenProvider/AcceptanceConfig):
   - `AddonOnboardingAcceptanceIT`: onboarding order with addon -> FULFILLED -> quota includes
     addon delta -> bill run -> invoice carries the addon line.
   - `StandaloneAddonPurchaseAcceptanceIT`: active subscription -> ADDON order -> payment ->
     FULFILLED -> quota topped up.
   - `PlanChangeAcceptanceIT`: plan-change order -> payment -> tariffCode/version changed ->
     quota re-provisioned -> FULFILLED -> next bill run bills the new fee.
   - Regression: two-TARIFF order still fails activation with compensation.
2. Full fresh-stack E2E (thermal rules: code with stack down, one contiguous stack-up window,
   caffeinate-wrapped long commands, `make infra-down` immediately after): `make infra-destroy`,
   rebuild images, boot, register connectors, full acceptance sweep (AC-01/02/03 + campaign +
   BFF + the three new ITs), browser onboarding WITH addon selection, Swagger spot-check on the
   7 newly-enabled services, payments Idempotency-Key replay check.
3. Closeout: sprint README + STATUS.md, event-catalog.md, api-contracts (order, payment,
   customer, product-catalog, subscription), `docs/product/requirements.md` traceability,
   lessons.md if corrections arise.

## Acceptance criteria

- One green sweep containing every suite; run report appended to this sprint's README.
- All exit criteria in the sprint README met.
