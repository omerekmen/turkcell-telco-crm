# 24.7 - API Polish: Swagger, Sort Param, Quota-Exceeded Template

Status: DONE. Owner: domain-engineer + devops. Spec refs: Tech stack "Her servis ayri Swagger UI",
Section 12 pagination `sort`, AC 14.3 step 15 (ek paket onerisi iceren SMS).

## Tasks

1. Swagger: add `org.springdoc:springdoc-openapi-starter-webmvc-ui` to the 7 missing poms
   (order, payment, billing, usage, ticket, notification, campaign); permit `/swagger-ui/**`,
   `/swagger-ui.html`, `/v3/api-docs/**` in each service's security config where missing
   (gateway already permits).
2. Sort param: optional `sort=field,asc|desc` (default `createdAt,desc`) on the main list
   endpoints (orders, customers, invoices, tariffs, subscriptions, tickets, notifications);
   per-handler whitelist of sortable properties; invalid field -> platform validation error.
3. QUOTA_EXCEEDED SMS template: body becomes "Your data quota for subscription
   {{subscriptionId}} is exhausted. Purchase a data add-on or upgrade your package to continue.";
   `TemplateSeeder` becomes update-if-different so pre-existing DB rows get the new text.

## Acceptance criteria

- `/swagger-ui.html` renders on every Spring service port (12 services).
- Lists accept and apply sort; invalid sort field -> 400 with platform error shape.
- Fresh AND pre-existing notification DBs both carry the new template text (seeder test covers
  update path).
